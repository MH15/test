<template>
    <div id="menu">M</div>
    <div id="grid-sidebar">Matt Hall</div>
</template>

<style lang="scss">
    #menu {
        grid-row: header;
        grid-column: menu;
    }

    #grid-sidebar {
        grid-row: start / span 2;
        grid-column: menu;
        writing-mode: vertical-rl;
        text-orientation: mixed;
        font-weight: 500;
        text-transform: uppercase;
    }
</style>

<script></script>